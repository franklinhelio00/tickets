/* This code is importing the `App` class from a file called "app" and creating a new instance of the
`App` class. It then calls the `listen` method on the `app` instance. */
import { App } from "./app";

const app = new App

app.listen();